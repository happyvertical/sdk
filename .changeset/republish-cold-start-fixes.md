---
'@happyvertical/documents': patch
'@happyvertical/messages': patch
---

Republish: npm 0.89.10 was cut from a head that predates the `uuid` removal in `@happyvertical/documents` (#1256) and the ESM import fix in `@happyvertical/messages` (#1257). This release carries both.
