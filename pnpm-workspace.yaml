packages:
  - 'packages/*'
  - 'docs'
  - 'test-town-gnode'
  - '../bentleyalberta.com'

# pnpm workspace configuration for HAVE SDK monorepo
# Infrastructure packages now in packages/* (cache, config, documents, etc.)
# Modules packages in packages/modules/* (tags, places, profiles, etc.)