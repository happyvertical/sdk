{
  "name": "have-sdk",
  "private": true,
  "author": "Will Griffin <willgriffin@gmail.com>",
  "type": "module",
  "version": "0.0.3",
  "scripts": {
    "format": "prettier --write .",
    "format-check": "prettier --check .",
    "prepare": "husky",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "clean": "pnpm -r exec rm -rf dist && pnpm -r exec rm -rf tsconfig.tsbuildinfo",
    "build": "pnpm clean && pnpm --filter @have/utils build && pnpm --filter @have/files build && pnpm --filter @have/spider build && pnpm --filter @have/sql build && pnpm --filter @have/pdf build && pnpm --filter @have/ai build && pnpm --filter @have/smrt build && pnpm --filter @have/svelte build",
    "dev": "pnpm -r build --watch",
    "publish-packages": "pnpm build && changeset publish",
    "release": "standard-version --no-verify",
    "postrelease": "node scripts/update-package-versions.cjs"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.12",
    "conventional-changelog-cli": "^5.0.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.4.2",
    "prettier": "^3.4.2",
    "standard-version": "^9.5.0",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  },
  "resolutions": {
    "@types/node": "22.13.0"
  },
  "engines": {
    "node": "22.x"
  }
}
